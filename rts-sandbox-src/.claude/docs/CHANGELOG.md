# История замысла

Строка на изменение: что было, что стало, источник. Подробности — в git.
Файл ведётся с `/adopt`; всё, что было до него, лежит в истории коммитов.

## 2026-09-05

- `/adopt`: доки собраны из кода. 18 механик M-001..M-018 со статусом
  `implemented`, 20 задач в бэклоге, источник — код, не замысел.
- 01-vision, 02-scope: `proposed` → `accepted`. Было «во что играем,
  неизвестно» → стало «движок RTS, на котором собираются игры жанра,
  как кастовые карты Warcraft III». Источник — концепт «начало работы и
  основные идеи по проекту».
- 02-scope: мультиплеер и мобильные платформы были «снаружи навсегда» →
  стали заявленным будущим, которое учитывается при проектировании в
  мягком виде. Источник — концепт и ответ в чате на вопрос 6.
- 07-smoke-check: было «до ~15 пунктов, регресс после задач» → стало
  «1-2 пункта на крупную механику, проверка в конце версии». Список
  сокращён с 15 до 9. Источник — ответ в чате.
- M-015 способности: `implemented` → `accepted`, переписана целиком.
  Было «каст в точку, один тип действия» → стало «три типа каста, эффекты
  с длительностью, подсказки прицеливания, данные в ScriptableObject,
  логика вне». Источник — концепт.
- M-019 эффекты на юните: заведена. Источник — концепт (яд, стан).
- M-020 прицеливание и подсказки: заведена. Источник — концепт.
- M-005 поведения: остаётся `implemented`, но помечена как переписываемая
  в T-021 (паттерн Strategy). Источник — концепт и ответ в чате.
- M-011 производство: было «точка сбора не сделана» в границах → стало
  ссылкой на заявленную задачу T-030. Источник — концепт.
- Состав версий 0.1.1-alpha и 0.2.0-alpha задан пользователем в концепте
  и уточнён в чате: Strategy переехала из 0.2.1 в начало 0.1.1, из-за
  чего версия 0.2.1-alpha осталась без содержания и не заводилась.
- ID версий в пайплайне — `v0.1.1` и `v0.2.0`, без суффикса `-alpha`:
  валидатор принимает только `vX.Y.Z`. Полное имя живёт в заголовке
  версии, в git-ветке и в `bundleVersion`.
- Каст в область: решён наследованием от каста в точку. Источник — ответ
  в чате на вопрос 2.
- Оглушение: эффект включает поведение, снятие эффекта убирает поведение;
  приказы во время оглушения принимаются и применяются после выхода.
  Второе отменяет формулировку концепта «нельзя отдавать приказы».
  Источник — ответ в чате на вопрос 3.
- Клавиша `S` зарезервирована под приказ «стоп», перестаёт быть доступной
  для способностей. Источник — ответ в чате на вопрос 5.
- M-019 эффекты: тождество эффекта определено по ID способности, кастер не
  важен. Источник — ответ в чате, Q-9.
- M-020 прицеливание: подсказка не показывается, пока каст невозможен;
  возникает в момент конца кулдауна. Источник — ответ в чате, Q-10.
