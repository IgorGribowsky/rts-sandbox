# Открытые вопросы

Формат жёсткий: доска читает строки `## Q-N` и поля под ними.
Проза ниже полей — свободная, скрипт её не трогает.

## Q-1 · Во что это должно играться
status: answered
for:
asked: 2026-09-05
answered: 2026-09-05

**Ответ в чате 2026-09-05:** «я пока разрабатываю движок, а не определённую
игру». Это вариант B: ценность сейчас в том, чтобы механики работали
правильно, а не в том, чтобы закрыть партию. Сказано в разговоре про T-020,
поэтому если имелось в виду что-то более узкое — поправь, перепишу.

Следствия, которые из этого беру: условие победы (T-009) и ИИ противника
(T-010) остаются вне ближайшего скоупа осознанно, а не по забывчивости;
`02-scope.md` переписан под «движок, а не игра».

Из кода видно RTS с базой, экономикой на двух ресурсах, лимитом населения и
одним героем-кастером на 1100 HP среди юнитов на 100-400. Чего в коде не
видно — зачем всё это. Варианты:

- **A. Классическая RTS-сессия** в духе Warcraft III: строишь базу, копишь
  армию, ломаешь чужую. Кастер — герой, вокруг него микро.
- **B. Песочница-полигон** для механик: победы нет и не нужно, ценность в
  том, чтобы обкатывать системы. Название проекта (`rts-sandbox`) и
  стартовые 300000 золота говорят скорее об этом.
- **C. Что-то третье**, чего в коде ещё нет.

Рекомендация: **B на сейчас, A как цель**. Тогда условие победы и ИИ
противника едут в бэклог осознанно, а не как забытое.

## Q-2 · Куда идём после 0.1.1-alpha
status: deferred
for:
asked: 2026-09-05
answered:

**Отложено 2026-09-05 (ответ в чате):** «потом план распишу на версии.
Сейчас мы пока пытаемся внедрить пайплайн и запланировать работу».
Сам не выдумываю и больше не поднимаю — вернёмся по `/ask`.

Содержание самой 0.1.1-alpha пользователь назовёт сам. Вопрос про то, что
за ней. Варианты ближайшей крупной цели:

- **A. Играбельная партия**: условие победы, простой ИИ противника, рестарт.
  Даёт первую версию, в которую можно позвать постороннего.
- **B. Достроить систему способностей**: каст в цель, пассивки, яд,
  несколько кастеров. Даёт глубину бою, но партии по-прежнему нет.
- **C. Интерфейс**: HUD ресурсов, панель выделения, кнопки построек.
  Снимает главное неудобство — сейчас отказы уходят только в консоль.

Рекомендация: **C, потом A**. Без HUD любой плейтест упирается в то, что
игрок не понимает, почему ничего не строится.

## Q-3 · Что из найденного временное, а что окончательное
status: answered
for: T-017
asked: 2026-09-05
answered: 2026-09-05

**Ответ в чате 2026-09-05:** «да пусть будут отладочные, если ты про
фпс-трекер и тестовые сценарии, они не мешают».

Значит: `TestScenarios`, `FPSTracker`, `FriendlyFire` и стартовые 300000
золота остаются в сцене как есть. Это инструмент движка, а не мусор.
`Assets/_Recovery/` отдельно не обсуждался — по умолчанию тоже оставляю,
он ничему не мешает.

T-017 при этом не отменяется, но она не про «убрать трекер»: сам трекер
остаётся, меняется только то, что он пишет в консоль каждый кадр. Причина
в задаче.

Отладочное в стартовой сцене. По каждому пункту нужно решение: оставляем
как есть, прячем за флаг, или убираем.

- стартовые ресурсы 300000 золота / 100000 дерева;
- `FriendlyFire` включён;
- `FPSTracker` печатает FPS в консоль каждый кадр;
- `TestScenarios` висит на горячих клавишах в игровой сцене;
- `Assets/_Recovery/0.unity` — папка от восстановления проекта.

Рекомендация: ресурсы и `FriendlyFire` оставить как есть до баланса;
`FPSTracker` заменить на экранный счётчик или выключить; `TestScenarios`
оставить, но убрать из билда позже; `_Recovery` удалить.

**Частично закрыто 2026-09-05 (ответ в чате).** На перечень отладочного в
сцене ответ был «не понял проблемы». С учётом Q-1 (движок, а не игра) это
разумно: `TestScenarios`, `FriendlyFire` и стартовые ресурсы для полигона
не мусор, а инструмент. T-017 сузилась до единственного пункта, который
реально мешает, — `Debug.Log` каждый кадр.

Остаётся открытым только про `Assets/_Recovery/0.unity`: это папка от
восстановления проекта, в билд не входит, но лежит в репозитории. Удалять
или оставить?

## Q-4 · Типы урона: доводить или выкинуть
status: answered
for: T-006
asked: 2026-09-05
answered: 2026-09-05

**Ответ в чате 2026-09-05:** вариант A, доводить. «В будущем это будет
важно, так как будет броня и сопротивление магии, возможно разные типы
защиты». Enum остаётся, к нему добавляется сторона защиты. Подробности —
в T-006, флаг `needs-design` там снят.

`DamageType` (Normal / Pierce / Magic / Siege) проставлен на всех юнитах и
зданиях, но ни на что не влияет: `CanBeAttacked` игнорирует тип, таблицы
брони нет. Варианты:

- **A. Довести**: добавить броню юниту и матрицу «тип урона против брони».
  Это отдельная механика и заметная работа по балансу.
- **B. Оставить как есть** — поле живёт про запас, документируем что не
  работает.
- **C. Выкинуть** enum и поля.

Рекомендация: **B** до того, как появится условие победы. Матрица брони без
партии не проверяется плейтестом.

## Q-5 · Задачи в GitHub Issues
status: answered
for:
asked: 2026-09-05
answered: 2026-09-05

**Ответ 2026-09-05:** issues неактуальны. Переносить нечего, доска одна —
`.claude/tasks/`. Номера `#NN` в старых коммитах остаются как история.

**Переформулировано 2026-09-05:** первый заход был непонятен.

Факт, из которого вопрос вырос: в истории коммитов задачи идут по номерам
issue — `Implemented Wave spell #65 and Blink spell #66`, `Implemented
impact system #64`, `Done Don't select buildings with units #26`. То есть
у проекта уже есть доска задач на GitHub, и она работала до пайплайна.

Теперь досок две: старая на GitHub и новая в `.claude/tasks/`. Вопрос
только про **открытые** issues — закрытые никого не волнуют.

- **A. Перенести открытые issues в `.claude/tasks/`** и дальше вести всё
  здесь. GitHub остаётся для истории. Нужно, чтобы ты показал список
  открытых issues — я разберу их батчем и заведу задачами.
- **B. Оставить как есть:** старое доживает на GitHub, новое заводится в
  пайплайне. Пока старые не кончатся, смотреть надо в оба места.
- **C. Наоборот:** пайплайн для доков и планирования, задачи по-прежнему
  на GitHub. Тогда `.claude/tasks/` почти не используется, и половина
  команд (`/next`, `/task`, `/done`, доска) остаётся без работы.

Рекомендация: **A**, если открытых issues немного. Две доски расходятся
быстро: задача закрыта в одном месте и висит в другом, и непонятно, какое
из двух состояний правда. Если открытых issues много и часть из них
неактуальна — тоже A, но сначала выкинуть неактуальные.

Если issues уже нет или ты ими давно не пользуешься — просто скажи,
и вопрос закроется как неактуальный.

## Q-6 · Каст в цель и пассивные способности
status: answered
for:
asked: 2026-09-05
answered: 2026-09-05

**Ответ в чате 2026-09-05,** по каждой части отдельно:

- каст в цель (T-002) — задел на будущее, направленные скиллы и скиллы на
  юнита планируются, но не сейчас;
- пассивки (T-003) — нужны, двух видов: постоянный бонус и срабатывание по
  обстоятельству. Дизайн-вопрос снят, задача разблокирована;
- яд (T-001) — делаем: периодический урон, разные эффекты параллельно,
  тот же эффект обновляет длительность.

То есть по факту не A и не B, а «яд и пассивки в работу, каст в цель на
будущее».

**Дополнено 2026-09-05 (ответ в чате):** «основной костяк способностей
должен быть сделан в текущей версии». Это первое, что названо в скоуп
0.1.1-alpha.

Здесь расхождение с более ранним ответом того же дня, поэтому фиксирую
оба: про T-002 (каст в юнита) сначала было сказано «это на будущее», а
«костяк способностей» его вроде бы включает.

**Уточнено 2026-09-05:** состав костяка пользователь опишет сам, файлом в
`concept/`. То есть дальше не `/ask`, а `/intake`: разбираю концепт,
раскладываю по докам и задачам, и только после этого `/plan`. Сам состав
не выдумываю и вопрос больше не поднимаю.

Система способностей заложена под три вида, реализован один (каст в точку).
`SkillCastingToTargetBehaviour` закомментирован, `PassiveSkill` без
исполнителя, `PoisonDamageImpact` бросает `NotImplementedException`.

- **A. Это следующая работа** — доделываем в ближайшей версии.
- **B. Заложено на будущее** — уезжает в бэклог, заглушки остаются.

Рекомендация: **B**, кроме `PoisonDamageImpact`: исключение из ассета,
который выглядит рабочим, — ловушка при плейтесте.

## Q-7 · Противник
status: answered
for: T-010
asked: 2026-09-05
answered: 2026-09-05

**Ответ 2026-09-05:** вариант A — движку ИИ пока не нужен, `TestScenarios`
для проверки механик хватает. T-010 остаётся в бэклоге до появления
конкретной игры, сам её больше не поднимаю.

**Переформулировано 2026-09-05** с учётом Q-1: разрабатывается движок, а
не игра, поэтому вопрос не «какой противник нужен игре», а «нужен ли
движку модуль ИИ и в каком объёме».

Факт: вражеские команды сейчас полностью пассивны. Юниты отбиваются
автоатакой и зовут союзников на помощь, но никто не строит, не производит
и не идёт в атаку. Проверить механики в бою можно только руками, через
`TestScenarios` на горячих клавишах.

- **A. ИИ движку пока не нужен.** `TestScenarios` для проверки механик
  хватает. T-010 остаётся в бэклоге до появления конкретной игры.
- **B. Простой ИИ как ещё один модуль движка:** волны юнитов по таймеру
  или примитивное «собрал группу — пошёл в атаку». Без экономики.
  Даёт возможность проверять механики в движении, без ручного тыканья.
- **C. Полноценный ИИ со своей базой и экономикой.** Здесь важное
  техническое препятствие: `PlayerResources` висит на объекте с тегом
  `PlayerController`, и в сцене он ровно один — на команде игрока. Куча
  кода (`Building`, `HeldMine`, `UnitProducing`, `GridSegment`,
  `UnitCommandManager`, `HarvestedResource`) находит его через
  `FindGameObjectWithTag` и возьмёт первый попавшийся. Второй игрок
  потребует сначала переделать эту привязку.

Рекомендация: **A сейчас, B когда костяк способностей будет готов.**
C не начинать, пока не решено, нужен ли вообще второй набор ресурсов:
это переделка, которая заденет шесть систем.

## Q-8 · Дерутся ли нейтралы между собой
status: answered
for: T-018
asked: 2026-09-05
answered: 2026-09-05

**Ответ 2026-09-05,** шире, чем спрашивалось: не вариант из списка, а
полные правила союзничества.

1. Всё, что работает сейчас, работает так же.
2. `Team.IsNeutral` начинает работать: такая команда — союзник **всем**,
   вообще не глядя на команды и альянсы.
3. Команда без альянса считается как будто она одна в своём альянсе:
   союзник только себе, враг всем, кроме команд с `IsNeutral`.

Спека и порядок работы — в T-018, флаг `needs-design` там снят.

Две формулировки того же дня — «нейтральные существа, которые против
всех» и «IsNeutral считаются всем союзниками» — это две разные сущности:
мирные нейтралы это `IsNeutral`, агрессивные это обычная команда без
альянса. **Подтверждено в чате 2026-09-05,** вопрос закрыт полностью.

Про нейтралов уже известно, что они против всех игроков — это задумано.
Открыт узкий остаток: враждебны ли нейтралы **друг другу**. Сейчас по коду
да: команда 99 не входит ни в один альянс, поэтому её список союзников пуст
включая её саму, и два нейтральных существа рядом начнут драться.
Не выстреливает только потому, что юнитов команды 99 в сцене нет.

- **A. Нейтралы своих не трогают.** Чинится тем, что `GetAllyTeams` всегда
  включает саму команду. Одна строка, поведение остальных команд не меняется.
- **B. Нейтралы дерутся со всеми, включая друг друга.** Тогда это не баг,
  T-018 закрывается как `cancelled`, а поведение стоит записать в M-009,
  чтобы следующий читатель кода не принял его за ошибку.
- **C. Нейтралы бывают разные:** мирная фауна и агрессивные существа.
  Тогда нужен не альянс, а признак на команде или на юните — флаг
  `Team.IsNeutral` как раз объявлен и не используется.

Рекомендация: **A**. Стая нейтралов, выкашивающая сама себя до подхода
игрока, — самый вероятный неприятный сюрприз. C заводить, когда появятся
конкретные нейтральные существа, а не заранее.

## Q-9 · Чем считается «тот же эффект» при повторном наложении
status: open
for: T-001
asked: 2026-09-05
answered:

Правило известно: разные эффекты действуют параллельно, тот же —
обновляет длительность. Не решено, что значит «тот же».

- **A. Пара «эффект + кастер».** У каждого мага свой яд на цели. Два мага
  травят одну цель — два независимых яда, и поддержка союзника не сбивает
  чужой отсчёт.
- **B. Только эффект, кастер не важен.** Один яд на цели, любой маг
  обновляет его длительность. Два мага дают не больше урона, чем один.
- **C. Пара «эффект + способность».** Яд от пассивки и яд от снаряда —
  разные, но два одинаковых снаряда от разных магов — один.

Рекомендация: **A**. При B поддержка союзника обнуляет чужой яд, и это
выглядит как баг, даже когда сделано нарочно. A дороже по памяти, но
предсказуемо.

## Q-10 · Показывать ли подсказку, когда каст невозможен
status: open
for: T-024
asked: 2026-09-05
answered:

Кулдаун не прошёл или маны не хватает, а игрок зажал клавишу.

- **A. Подсказка не показывается вовсе.** Молчание = нельзя.
- **B. Показывается, но другим цветом.** Игрок видит дальность и целится
  заранее, к моменту конца кулдауна уже наведён — и это прямо
  поддерживает отзывчивость: зажатая клавиша сработает сама, когда
  кулдаун кончится.
- **C. Показывается как обычно,** каст просто не происходит.

Рекомендация: **B**. Она склеивается с уже принятым правилом про зажатую
клавишу: игрок держит клавишу, целится, и каст уходит в тот момент, когда
стал возможен. При A он в этот момент ничего не видит.
